import cv2

x_coordinates = input(input("Enter the X coordinates :"))
y_coordinates = int(input("Enter the Y coordinates: "))

goal = (y_coordinates,x_coordinates)

















# import cv2
# import matplotlib.pyplot as plt
#
# # Load the image
# image = cv2.imread("3D_cleaned_final.png", cv2.IMREAD_GRAYSCALE)
#
# # Define known pixel coordinates of the rover
# start = (742,407)  # Replace with actual pixel coordinates
# goal = (358,675)
#
# # Plot the image with the rover's start position
# plt.imshow(image, cmap="gray")
# plt.scatter(start[1], start[0], color='red', marker='o')  # (x, y) in Matplotlib
# plt.scatter(goal[1], goal[0], color='blue', marker='o', label="Goal")   # Blue for goal
# plt.show()


